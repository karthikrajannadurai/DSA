package org.dsa.Graph;

/**
 * Same code as adjacent list dfs, except we need to run dfs for each and every node.
 * for cycle detection we need to keep track of the current path, and we need to check for existent
 * of that node.
 */
public class TopologicalSort {

}
