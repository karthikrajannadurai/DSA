package org.dsa.Tree.BinaryTree;

public class HeightOfBinaryTree {

    // To find the height of the binary tree and level of binary tree is same.
    // to find the height, use level order traversal with level loop.
    // level should be starting from 0.
    // height of node is different from level of node
    // depth is edges from root to particular node.
    // height is max edges from root to leaf node.

    // depth -- 0
    // height --3 stackoverflow image
    // depth of node is same as level of node.
    // consider binary tree is buried in ground, root node is at top, so depth is 0,
    // so going level 1 , depth is increased by 1.
}
